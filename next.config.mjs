/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
          {
           
            hostname: 'www.caltextraders.com',
           
          },
        ],
      },
};

export default nextConfig;
